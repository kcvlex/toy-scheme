(ocamllex lexer)

(menhir
  (modules parser))
  
(library
  (libraries util)
  (modules_without_implementation symbolType
                                  astType
                                  cpsType
                                  aNType
                                  interlangType
                                  refType
                                  closureType)
  (name compiler))

(env
  (dev
    (flags (:standard -warn-error -A))))
